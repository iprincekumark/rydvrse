<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.rydvrse</groupId>
        <artifactId>rydvrse-platform</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>rydvrse-app</artifactId>
    <name>RYDVRSE Application</name>
    <description>Main Spring Boot Application — assembles all modules</description>

    <dependencies>
        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- Flyway -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <!-- flyway-core is managed by Spring Boot BOM -->

        <!-- API Docs -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>

        <!-- All RYDVRSE Modules -->
        <dependency>
            <groupId>com.rydvrse</groupId>
            <artifactId>rydvrse-shared</artifactId>
        </dependency>
        <dependency>
            <groupId>com.rydvrse</groupId>
            <artifactId>rydvrse-auth</artifactId>
        </dependency>
        <dependency>
            <groupId>com.rydvrse</groupId>
            <artifactId>rydvrse-customer</artifactId>
        </dependency>
        <dependency>
            <groupId>com.rydvrse</groupId>
            <artifactId>rydvrse-driver</artifactId>
        </dependency>
        <dependency>
            <groupId>com.rydvrse</groupId>
            <artifactId>rydvrse-trip</artifactId>
        </dependency>
        <dependency>
            <groupId>com.rydvrse</groupId>
            <artifactId>rydvrse-payment</artifactId>
        </dependency>
        <dependency>
            <groupId>com.rydvrse</groupId>
            <artifactId>rydvrse-safety</artifactId>
        </dependency>
        <dependency>
            <groupId>com.rydvrse</groupId>
            <artifactId>rydvrse-notification</artifactId>
        </dependency>
        <dependency>
            <groupId>com.rydvrse</groupId>
            <artifactId>rydvrse-admin</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
