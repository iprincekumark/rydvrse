spring:
  datasource:
    url: jdbc:h2:mem:rydvrse;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379

rydvrse:
  security:
    jwt-secret: test-test-test-test-test-test-test
  auth:
    default-otp-code: 123456
  payments:
    webhook-secret: test-webhook-secret
